enum Role { admin, customer, housekeeper }

enum AccountStatus { active, inactive }

enum BookingStatus { pending, confirmed, cancelled }

enum PaymentStatus { unpaid, paid, refunded }

enum DiscountType { amount, percent }

enum ReviewTarget { service, housekeeper }

enum TaskClaimStatus { claimed, completed, cancelled }